<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.demoapp"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:name=".DemoApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.TextNoteActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.activity.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.MainActivity"/>
        </activity>

        <activity
            android:name=".ui.activity.VideoNoteActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.activity.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.MainActivity"/>
        </activity>

        <activity
            android:name=".ui.activity.VideoListActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/video_list_activity_title"
            android:parentActivityName=".ui.activity.VideoNoteActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.VideoNoteActivity"/>
        </activity>



    </application>

</manifest>